import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF00D4FF);
  static const Color secondary = Color(0xFF7B2FFF);
  static const Color accent = Color(0xFFFF6B35);
  
  static const Color background = Color(0xFF050A14); // --dark
  static const Color cardBg = Color(0xFF0A1628); // --dark2
  static const Color dark3 = Color(0xFF0F2040); // --dark3
  
  static const Color textPrimary = Color(0xFFE8F4FF); // --text
  static const Color textSecondary = Color(0xFF7A9EC0); // --muted
  
  static const Color flutter = Color(0xFF54C5F8); // --flutter
  
  static const Color whatsapp = Color(0xFF25D366); // Keep for backwards compatibility if needed
}
